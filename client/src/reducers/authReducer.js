export default function(state = {}, action) {
  switch (action.type) {
    default:
      return state
  }
}

// state : initial data (previous state)
// action : function that does something with the previous state/data

// Reducers : takes a set of input and returns one value.
// https://stackoverflow.com/questions/40599496/why-is-a-redux-reducer-called-a-reducer
